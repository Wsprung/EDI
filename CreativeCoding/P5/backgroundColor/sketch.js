function setup() {
  createCanvas(windowWidth, windowHeight); /*This creates the window
  width and height to be the size of my browser window*/
  background(10, 46, 46); //background(R, G, B)

}

function draw() {
}
